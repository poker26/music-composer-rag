# Legacy audio pipeline: ingestion (ingest.py), similarity search (search.py),
# and the Gradio web UI (app.py). Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-audio.txt
# Also requires the ffmpeg system binary (apt install ffmpeg) and a running
# Qdrant instance (see config/settings.py / QDRANT_HOST).
librosa>=0.10.0
basic-pitch>=0.3.0
qdrant-client>=1.7.0
soundfile>=0.12.0
laion-clap>=1.1.4
gradio>=4.0.0
