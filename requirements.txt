# Core (path C: LLM composer + MIDI). This is all you need to run generate.py.
anthropic>=0.40.0
mido>=1.3.0
numpy>=1.24.0

# Audio analysis / search / web UI stack (ingest.py, search.py, app.py) lives in
# requirements-audio.txt — install it only if you need the legacy audio pipeline.
